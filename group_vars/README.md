# group_vars

This directory contains group-level variables for Ansible inventories.

Files in this directory are loaded automatically based on inventory
group names.

Variables defined here apply to all hosts in the corresponding group.

Use Ansible Vault for sensitive values. Do not store plaintext secrets
in version control.
